# Orchestration: Slit (UI Dev) - Elevated Drag-Drop Diagnosis

**Timestamp:** 2026-05-31T23:57:03Z (UTC)  
**Agent:** Slit  
**Task:** WPF drag-drop elevated failure diagnosis & architecture decision

## Summary

Slit diagnosed a persistent WPF drag-drop failure in the elevated (Restart as Administrator) scenario. Root cause identified: UIPI blocks OLE COM channel when app is elevated but drag source (Explorer) is not. `ChangeWindowMessageFilterEx` only fixes legacy `WM_DROPFILES` path, not WPF's OLE-based drag-drop.

## Changes Committed

| File | Change |
|------|--------|
| `src/EWSR_PMR_ModApp.UI/MainWindow.xaml.cs` | Added runtime diagnostics: elevation status, ChangeWindowMessageFilterEx return values, handler firing, GetFormats dump, IsZipDrop result. Logs to `%TEMP%\ewsr_dragdrop.log`. Added TODO pointing to helper-process architecture. |
| `src/EWSR_PMR_ModApp.UI/MainWindow.xaml` | Hardened drop zone hit-test: `IsHitTestVisible="False"` on decorative TextBlocks. |

## Build Status

✓ Successful (no build errors)

## Decision Filed

**slit-elevated-dragdrop.md**: Proposes non-elevated-UI + elevated-helper architecture. Recommends Elliott capture diagnostic log data before proceeding with helper-process implementation.

## Requestor

Elliott Williams

---

**Scribe Note:** Decision and session log entries written. Inbox merged. Ready for follow-up based on diagnostic log data.
