<Window x:Class="EWSR_PMR_ModApp.UI.Dialogs.WarningsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Install Warnings"
        Height="420" Width="620"
        MinHeight="240" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Background="#0A0A0A"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#050505"
                Padding="16,12"
                BorderBrush="#E8E4D8"
                BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock x:Name="TitleText"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#C2A35A"/>
                <TextBlock Text="The following files were skipped and not installed (no matching location in the game data, or an unresolved mapping)."
                           FontSize="12"
                           Foreground="#FAB387"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Warnings list -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="16,8">
            <ItemsControl x:Name="WarningsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="0,6"
                                BorderBrush="#262626"
                                BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="⚠"
                                           Foreground="#FAB387"
                                           VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding}"
                                           Foreground="#EFEADD"
                                           FontSize="12"
                                           TextWrapping="Wrap"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                Background="#050505"
                BorderBrush="#E8E4D8"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <Button Content="OK"
                    Click="OK_Click"
                    HorizontalAlignment="Right"
                    Background="#C2A35A"
                    Foreground="#050505"
                    BorderBrush="#C2A35A"
                    FontWeight="SemiBold"
                    Padding="24,6"/>
        </Border>
    </Grid>
</Window>
